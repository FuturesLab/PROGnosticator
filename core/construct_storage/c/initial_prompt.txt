You are an expert in the C programming language.

Your task is to help design a diverse and representative set of code-level constructs for testing code translation tools (transpilers).
The goal is to cover a wide range of core constructs—including edge cases—to ensure transpilers correctly handle various usage patterns in C.

Generate a JSON object of C core constructs and their usages based on the following high-level categories as the key.

Below is a sample JSON object with two examples per category for reference only:

{
  "Data Types": [
    "basic int data type",
    "pointer to int type"
  ],
  "Casting": [
    "explicit casting between integers and floating-point types",
    "casting pointers to void* and back"
  ]
}

Do not simply repeat the sample above. 
Instead, expand each category to include distinct core construct and usage pattern found in the C language.
Ensure that each category contains exactly 2 distinct construct usages.
Don't add any new category.
Be sure to NOT include any construct usage that depends on external library/header file inclusion. Just focus on core constructs.


The output must be a valid JSON object containing exactly the keys listed above, with each key’s array comprehensively populated with explicit, non-redundant entries, and without any additional commentary or explanation.


