Write a small JavaScript program that demonstrates this: {CONSTRUCTS}
The program must include a function named func1() that MUST NOT take any function arguments, demonstrates the construct, and returns a value. 
If the construct is a function or similar definition (such as using an arrow function, a class, or an object literal), it must be defined outside func1(). 
func1() must only use or call it logically, and not define the construct inside itself. 
Include only a single comment at the top describing what construct the program is exercising. 
Do not include any other comments in the code. 
Do not provide any explanation, only the program. 
Do not wrap the code in triple backticks or include any language tag. 
The program must include a main function without any arguments, which saves the return value of func1() into a variable res, and prints the value of res. 
There MUST NOT be any other print statements. 
Just output the raw JavaScript code.