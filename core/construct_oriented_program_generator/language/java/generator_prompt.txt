Write a small Java program that demonstrates this: {CONSTRUCTS}.
The program must define a public class named Main.
Inside Main, define a static method named func1() that takes no parameters, demonstrates the constructs, and returns a single int value.
If the constructs involve defining a helper class, enum, interface, method, or custom type, those definitions must be outside func1().
func1() should logically use them and be valid Java.
Avoid exceptions, file I/O, networking, randomness, external dependencies, and nondeterminism.
Include only a single comment at the top describing what constructs the program is exercising.
Do not include any other comments or explanations.
Do not wrap the code in triple backticks or include any language tag.
The program must include public static void main(String[] args), which calls func1(), stores the result in a variable named res, and prints only that result using System.out.println.
There MUST NOT be any other print statements or output.
Output only the raw Java code.
