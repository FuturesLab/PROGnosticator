Write a small Python 3 program that demonstrates this construct: {CONSTRUCT}
The program must include a target function named func1() that MUST NOT take any function arguments, demonstrates the construct, and returns a single integer value related to the construct usage.
If the construct involves defining a helper function, class, decorator, or custom object, the definition must be outside func1(), and func1() must only use or apply it logically.
Avoid exceptions, file I/O, networking, randomness, external packages, and nondeterminism.
Include only a single comment at the top describing what construct the program is exercising.
Do not include any other comments in the code.
Do not provide any explanation, only the program.
Do not wrap the code in triple backticks or include any language tag.
The program must include a main guard, which saves the single return value of func1() into a variable res, and prints the value of res.
There MUST NOT be any other print statements.
Just output the raw code.
