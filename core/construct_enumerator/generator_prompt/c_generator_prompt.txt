Write a small C program that demonstrates this construct: {CONSTRUCT}
Add necessar headers like stdio for printing.
The program must include a target function named func1() that MUST NOT take any function arguments,
demonstrates the construct, and MUST return a single integer value related to the construct usage.
If the construct involves defining a new function, struct, or custom type, the definition must be outside func1(),
and func1() must only use or apply it logically.
Include only a single comment at the top describing what construct the program is exercising.
The program must include a main function without any arguments, which saves the single return value of func1() into a variable res, and prints the value of res.
There MUST NOT be any other print statements.
Avoid any kind of undefined behavior (UB), including but not limited to:
out-of-bounds access, use of uninitialized variables, division by zero, invalid pointer dereferencing,
or any operation resulting in implementation-defined or unspecified behavior in C.
Do NOT include any other comments in the code.
Do NOT provide any explanation, only the program.
Do NOT wrap the code in triple backticks or include any language tag.
Just output the raw code.